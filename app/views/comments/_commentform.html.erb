<%= simple_form_for @comment, url: { action: "create" } do |f| %>

  <%= f.text_area :text, label: false, class: "form-control" %>

  <%= f.submit class: "btn btn-primary btn-lg btn-group-justified" %>

<% end %>
